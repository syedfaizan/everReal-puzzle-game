# everReal-puzzle-game
steps to run

1. Clone the repo
2. run `npm intall` in repo directory
3. run `npm start`
4. visit port `5000` for the UI

# run tests
1. run `npm run test` to run unit tests of the game engine
